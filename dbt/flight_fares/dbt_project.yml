name: "flight_fares"
version: "1.0.0"
config-version: 2

profile: "flight_fares"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  flight_fares:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
